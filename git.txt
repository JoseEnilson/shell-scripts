# INSTALANDO O GIT:
  sudo apt install git -y

# CONFIGURANDO O GIT:
  git config --global user.name "Jose Enilson"
  git config --global user.email "mootttaaaa@gmail.com"
  git config --global core.editor vim
  git config user.name
  git config user.email
  git config --list
  
# INICIALIZAR O REPOSITÓRIO:  
  git init
  
 # TRABALHANDO COM GIT:
  vim Readme.md
  git status
  git add Readme.md 
  git commit -m "Add Readm.md" 
  git status
  git log
  git log --decorate
  git log --author="Jose enilson"
  git log --author="Jose Enilson"
  git shortlog
  git shortlog -sn
  git log --graph
  git show 75eba853b27d12023fe2eae848e3c1539b1ad1af
  git diff
  git diff --name-only
  git checkout Readme.md
  git reset HEAD Readme.md
  git reset --soft hashDoCommint
  git reset --soft hashDoCommint(um commit antes)
  git reset --mixed hashDoCommint(um commit antes)
  git reset --hard hashDoCommint(um commit antes)
  ssh-keygen -t rsa -b 4096 -C "mootttaaaa@gmail.com"
  git remote add origin git@github.com:JoseEnilson/shell-scripts.git
  git remotee -v
  git push -u origin master
  
# ENVIANDO ARQUIVOS PARA O GITHUB
  git add add_disk-melhores-praticas.sh remove_disk-melhores-praticas_sh resize_disk-melhores-praticas.sh 
  git status
  git commit -am "lvm config" 
  git push -u origin master
  
# CLONANDO REPOSITÓRIO:  
  git clone git@github.com:JoseEnilson/shell-scripts.git shell-scripts.git.clone
  
